name: garage_gate_app
description: App para controlo do portão da garagem
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI
  cupertino_icons: ^1.0.6
  flutter_svg: ^2.0.9
  
  # State Management
  provider: ^6.1.1
  
  # HTTP & WebSockets
  http: ^1.1.2
  web_socket_channel: ^2.4.0
  signalr_netcore: ^1.3.3
  
  # Local Storage
  shared_preferences: ^2.2.2
  
  # Firebase
  # firebase_core: ^3.0.0
  # firebase_messaging: ^15.0.0
  
  # Notifications
  # flutter_local_notifications: ^17.0.0
  
  # JSON
  json_annotation: ^4.9.0
  
  # Time formatting
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  build_runner: ^2.4.7
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/icons/
